# auto-generated by grc.converter

id: specest_moving_average_vff
label: Moving Average (Vector)
category: Spectrum Estimation

parameters:
-   id: length
    label: Averaging Length
    dtype: int
-   id: vlen
    label: Vector Length
    dtype: int
    hide: ${ 'part' if vlen == 1 else 'none' }
-   id: scale
    label: Scale
    dtype: float
-   id: max_iter
    label: Max. Iterations
    dtype: int
    default: '4096'

inputs:
-   domain: stream
    dtype: float
    vlen: ${ vlen }

outputs:
-   domain: stream
    dtype: float
    vlen: ${ vlen }

templates:
    imports: import specest
    make: specest.moving_average_vff(${length}, ${vlen}, ${scale}, ${max_iter})

file_format: 1
