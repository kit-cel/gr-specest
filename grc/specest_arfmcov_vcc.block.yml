# auto-generated by grc.converter

id: specest_arfmcov_vcc
label: Arfmcov vcc
category: SPECEST

parameters:
-   id: unsignedorder
    label: Unsignedorder
    dtype: raw
-   id: normalise
    label: Normalise
    dtype: int
    default: ' -1'
-   id: blocklen
    label: Blocklen
    dtype: int
    default: '0'
    hide: ${ 'part' if vlen == 1 else 'none' }

inputs:
-   domain: stream
    dtype: complex
    vlen: ${ blocklen }

outputs:
-   domain: stream
    dtype: complex
    vlen: ${ order+1 }
-   domain: stream
    dtype: float

templates:
    imports: import specest
    make: specest.arfmcov_vcc(${unsignedorder}, ${normalise})

file_format: 1
