# auto-generated by grc.converter

id: specest_arburg_vcc
label: AR-Burg Spectrum Estimator
category: Spectrum Estimation

parameters:
-   id: blocklen
    label: Block Length
    dtype: int
    default: 0
-   id: order
    label: Order
    dtype: int
-   id: normalise
    label: Normalisation Factor
    dtype: int
    default: -1
    hide: ${ 'part' if normalise == -1 else 'none' }

inputs:
-   domain: stream
    dtype: complex
    vlen: ${ blocklen }

outputs:
-   domain: stream
    dtype: complex
    vlen: ${ order+1 }
-   domain: stream
    dtype: float

templates:
    imports: import specest
    make: specest.arburg_vcc(${blocklen}, ${order}, ${normalise})

file_format: 1
