# auto-generated by grc.converter

id: specest_cyclo_fam
label: FAM Cyclic Spectrum Estimator
category: Spectrum Estimation

parameters:
-   id: Np
    label: First FFT length
    dtype: int
    default: '32'
    hide: ${ 'part' if vlen == 1 else 'none' }
-   id: P
    label: Second FFT length
    dtype: int
    default: '512'
-   id: L
    label: Oversampling Factor
    dtype: int
    default: '2'

inputs:
-   domain: stream
    dtype: complex

outputs:
-   domain: stream
    dtype: float
    vlen: ${ (2*Np) }

templates:
    imports: import specest
    make: specest.cyclo_fam(${Np}, ${P}, ${L})

file_format: 1
