# auto-generated by grc.converter

id: specest_adaptiveweighting_vff
label: Adaptiveweighting vff
category: SPECEST

parameters:
-   id: vlen
    label: Vlen
    dtype: raw
    hide: ${ 'part' if vlen == 1 else 'none' }
-   id: lambdas
    label: Lambdas
    dtype: real_vector
-   id: num_inputs
    label: Num_inputs
    dtype: int
    default: '2'
    hide: part

inputs:
-   domain: stream
    dtype: float
    vlen: ${ vlen }
    multiplicity: ${ num_inputs }

outputs:
-   domain: stream
    dtype: float
    vlen: ${ vlen }

templates:
    imports: import specest
    make: specest.adaptiveweighting_vff(${vlen}, ${lambdas})

file_format: 1
