# auto-generated by grc.converter

id: specest_fmcov
label: Fmcov
category: SPECEST

parameters:
-   id: fft_len
    label: FFT len
    dtype: int
    hide: ${ 'part' if vlen == 1 else 'none' }
-   id: fftshift
    label: Boolfftshift
    dtype: raw
    default: 'False'
-   id: decimation
    label: Decimation
    dtype: int
    default: ' 1'

inputs:
-   domain: stream
    dtype: complex

outputs:
-   domain: stream
    dtype: float
    vlen: ${ fft_len }

templates:
    imports: import specest
    make: specest.fmcov(${fft_len}, ${fftshift}, ${decimation})

file_format: 1
