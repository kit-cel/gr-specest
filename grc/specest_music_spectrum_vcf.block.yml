# auto-generated by grc.converter

id: specest_music_spectrum_vcf
label: music_spectrum_vcf
category: specest

parameters:
-   id: '...'
    label: '...'
    dtype: '...'

inputs:
-   domain: stream
    dtype: ''

outputs:
-   domain: stream
    dtype: ''

templates:
    imports: import specest
    make: specest.music_spectrum_vcf(${n}, ${m}, ${nsamples}, ${pspectrum_len})

file_format: 1
