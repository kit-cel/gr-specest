# auto-generated by grc.converter

id: specest_pad_vector
label: Pad Vector
category: Spectrum Estimation

parameters:
-   id: type
    label: Type
    dtype: enum
    options: [complex, float, int, short, byte]
    option_attributes:
        size: [gr.sizeof_gr_complex, gr.sizeof_float, gr.sizeof_int, gr.sizeof_short, gr.sizeof_char]
    hide: part
-   id: vlen_in
    label: Input Vector Length
    dtype: int
    hide: ${ 'part' if vlen == 1 else 'none' }
-   id: vlen_out
    label: Output Vector Length
    dtype: int
    hide: ${ 'part' if vlen == 1 else 'none' }

inputs:
-   domain: stream
    dtype: ${ type }
    vlen: ${ vlen_in }

outputs:
-   domain: stream
    dtype: ${ type }
    vlen: ${ vlen_out }

templates:
    imports: import specest
    make: specest.pad_vector(${type.size}, ${vlen_in}, ${vlen_out})

file_format: 1
