# auto-generated by grc.converter

id: specest_fcov
label: Fcov
category: SPECEST

parameters:
-   id: fft_len
    label: FFT_len
    dtype: raw
    hide: ${ 'part' if vlen == 1 else 'none' }
-   id: boolfftshift
    label: Boolfftshift
    dtype: raw
    default: ' false'
-   id: decimation
    label: Decimation
    dtype: int
    default: ' 1'

inputs:
-   domain: stream
    dtype: complex

outputs:
-   domain: stream
    dtype: float
    vlen: ${ fft_len }

templates:
    imports: import specest
    make: specest.fcov(${fft_len}, ${boolfftshift}, ${decimation})

file_format: 1
