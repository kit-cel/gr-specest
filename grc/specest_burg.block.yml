# auto-generated by grc.converter

id: specest_burg
label: Burg Spectrum Estimator
category: Spectrum Estimation

parameters:
-   id: block
    label: Input block length
    dtype: int
    default: '256'
-   id: fftlen
    label: FFT Length
    dtype: int
    default: '1024'
    hide: ${ 'part' if vlen == 1 else 'none' }
-   id: order
    label: AR model order
    dtype: int
    default: '5'
-   id: malen
    label: Moving Average Length
    dtype: int
    default: '8'
-   id: fftshift
    label: FFT Shift
    dtype: enum
    options: ['False', 'True']
    option_labels: [No shift, Shift DC to middle]
-   id: decimation
    label: Decimation
    dtype: int
    default: '1'

inputs:
-   domain: stream
    dtype: complex

outputs:
-   domain: stream
    dtype: float
    vlen: ${ fftlen }

templates:
    imports: import specest
    make: specest.burg(${blocklen}, ${fftlen}, ${order}, ${fftshift}, ${decimation})

file_format: 1
